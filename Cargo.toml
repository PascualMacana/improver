[package]
name = "mejorante"
version = "0.1.0"
edition = "2021"
description = "A constructor that mutates a piece of its own source and leaves a fitter child."
license = "MIT"
publish = false
repository = "https://github.com/PascualMacana/mejorante"

[[bin]]
name = "mejorante"
path = "src/main.rs"
